app:
  client_id: Validador
  grant_type: password
  password: admin
  urlAuth: http://${HOST_KC}:${PORT_KC}/realms/UsuariosCursoRealm/protocol/openid-connect/token
  username: admin
server:
  port: 8100
eureka:
  client:
    service-url:
      defaultZone: http://${HOST_EUREKA}:${PORT_EUREKA}/eureka
  instance:
    hostname: ${HOST}
